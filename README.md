# React

npm install -g netlify-cli

ntl init
 Yes, create and deploy project manually
? Team: SMVITM
? Project name (leave blank for a random name; you can change it later): lab3174
netlify build
netlify deploy
netlify deploy --prod

DONE!
